<div id="<%= dom_id post %>">
  <p>
    <strong>Image:</strong>
    <% if post.image.present? %>
      <%= image_tag post.image, alt: 'Image not found', onerror: "this.onerror=null;this.style.display='none';" %>
    <% else %>
      <span>No image available</span>
    <% end %>
  </p>

  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>

  <p>
    <strong>Description:</strong>
    <%= post.description %>
  </p>

</div>
